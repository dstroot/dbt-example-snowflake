packages:
  - package: calogica/dbt_expectations
    version: [">=0.8.0", "<0.9.0"]
    # <see https://github.com/calogica/dbt-expectations/releases/latest> for the latest version tag & usage

    # dbt-expectations is an extension package for dbt, inspired by the Great Expectations package 
    # for Python. The intent is to allow dbt users to deploy GE-like tests in their data 
    # warehouse directly from dbt